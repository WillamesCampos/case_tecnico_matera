version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: credit_track_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-credit_track_db}
      POSTGRES_USER: ${POSTGRES_USER:-credit_track_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-credit_track_password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-credit_track_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build:
      context: ..
      dockerfile: infrastructure/Dockerfile
    container_name: credit_track_web
    volumes:
      - ..:/app
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-credit_track_db}
      POSTGRES_USER: ${POSTGRES_USER:-credit_track_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-credit_track_password}
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:

